<%= form_with model: @business, local: true do |form| %>
 
  <% if @business.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@business.errors.count, "error") %> prohibited
        this business from being saved:
      </h2>
      <ul>
        <% @business.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </p>
 
  <p>
    <%= form.label :description %><br>
    <%= form.text_area :description %>
  </p>

   <p>
    <%= form.label :status %><br>
    <%= form.text_area :status %>
  </p>


  <p>
    <%= form.label :list_id %><br>
    <%= form.collection_select :list_id, current_user.lists.order(:name), :id, :name %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>