<!DOCTYPE html>
<html>
<head>
  <title>To Do</title>
</head>
<body link="red">

<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="businesses">To Do</a>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="businesses/new">New bussines</a></li>
        <li><a href="lists/new">New list</a></li>
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <li><a <%= link_to 'Log out', destroy_user_session_path, :method => :delete %></a></li>     
      </ul>
    </div>
  </div>
</nav>


<div class="row">
<% @lists.each do |list| %>
    <div class="col-sm-4">
      <table class = "table table-hover">
        <th><%= link_to "#{list.name}", list_path(list) %></th>
        <% list.businesses.each do |business| %>

          <tr>
            <td><%= link_to "#{business.name}", business_path(business) %></td>
            <td><%= business.status %></td>
            <td><%= business.priority %></td>
            <td><%= business.description %></td>
            <td><%= link_to image_tag("edit.png", size: "18x15"), edit_business_path(business) %></td>
            <td><%= link_to image_tag("destroy.png", size: "18x15"), business_path(business),
                                  method: :delete,
                                  data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </table>
    </div>
<% end %>
</div>
</body>
</html>