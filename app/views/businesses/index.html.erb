<h1>Your business</h1>

<%= link_to 'New business', new_business_path %><br>
<%= link_to 'New list', new_list_path %>


<% @lists.each do |list| %>
<table>

<th><%= link_to "#{list.name}", list_path(list) %></th>
   
  <% list.businesses.each do |business| %>
    <tr>
      <td><%= business.name %></td>
      <td><%= business.status %></td>
      <td><%= business.priority %></td>
      <td><%= business.description %></td>
      <td><%= link_to 'Show', business_path(business) %></td>
      <td><%= link_to 'Edit', edit_business_path(business) %></td>
      <td><%= link_to 'Destroy', business_path(business),
                                  method: :delete,
                                  data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  <% end %>


</table>